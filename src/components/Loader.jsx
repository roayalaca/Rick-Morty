import React from 'react';

const Loader = () => {
    return (
        <div className='loader'>
            <div class="spinner"></div>
            <p> Loading...</p>
        </div>
    );
};

export default Loader;